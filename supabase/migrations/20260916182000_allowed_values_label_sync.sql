-- Sync allowed_values with the option labels renamed by 20260916180000 and
-- 20260916181000: the validator checks facts against this array, so a renamed
-- option was reported as outside the field and hidden from the storefront.
-- Rerunnable: each update only touches arrays that still hold the old label.
-- Generated by scripts/inventory/compile_allowed_values_sync.py --version 20260916182000.
begin;
set local lock_timeout='5s';
set local statement_timeout='120s';
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Núcleo estriado de cassette'::text) then to_jsonb('Núcleo de cassette'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='hub_drive_receiver_kind' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Núcleo estriado de cassette'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Rosca para rueda libre'::text) then to_jsonb('Rosca para piñón (rueda libre)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='hub_drive_receiver_kind' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Rosca para rueda libre'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Rosca para piñón fijo y contratuerca'::text) then to_jsonb('Rosca para piñón fijo'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='hub_drive_receiver_kind' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Rosca para piñón fijo y contratuerca'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Otra interfaz OEM'::text) then to_jsonb('Otro'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='hub_drive_receiver_kind' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Otra interfaz OEM'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Rodamientos sellados'::text) then to_jsonb('Sellados'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='bearing_system' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Rodamientos sellados'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('SS / corta'::text) then to_jsonb('Corta (SS)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='derailleur_cage_length' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['SS / corta'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('GS / media'::text) then to_jsonb('Media (GS)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='derailleur_cage_length' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['GS / media'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('SGS / larga'::text) then to_jsonb('Larga (SGS)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='derailleur_cage_length' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['SGS / larga'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Combinación'::text) then to_jsonb('Clave (combinación)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='locking_mechanism' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Combinación'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Top pull'::text) then to_jsonb('Tiro arriba (top pull)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='front_derailleur_cable_pull' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Top pull'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Down pull'::text) then to_jsonb('Tiro abajo (down pull)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='front_derailleur_cable_pull' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Down pull'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Dual pull'::text) then to_jsonb('Doble tiro (dual pull)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='front_derailleur_cable_pull' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Dual pull'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Una pieza (americana / Ashtabula)'::text) then to_jsonb('Una pieza (americana)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='crankset_construction' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Una pieza (americana / Ashtabula)'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Dos piezas (eje solidario al brazo derecho)'::text) then to_jsonb('Dos piezas (integrado)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='crankset_construction' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Dos piezas (eje solidario al brazo derecho)'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Tres piezas (eje independiente)'::text) then to_jsonb('Tres piezas (motor aparte)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='crankset_construction' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Tres piezas (eje independiente)'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Orgánico'::text) then to_jsonb('Orgánico (resina)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='compound_type' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Orgánico'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Recto'::text) then to_jsonb('Recto (plano)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='bar_style' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Recto'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Presta (francesa)'::text) then to_jsonb('Francesa (Presta)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='valve_standard' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Presta (francesa)'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Schrader (americana / auto)'::text) then to_jsonb('Auto (Schrader / americana)'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='valve_standard' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Schrader (americana / auto)'::text]);
update public.spec_definitions d set allowed_values=(select jsonb_agg(case when e=to_jsonb('Llave + combinación'::text) then to_jsonb('Llave y combinación'::text) else e end order by ord) from jsonb_array_elements(d.allowed_values) with ordinality as t(e, ord)), updated_at=now() where d.tenant_id is null and d.key='locking_mechanism' and jsonb_typeof(d.allowed_values)='array' and d.allowed_values @> to_jsonb(array['Llave + combinación'::text]);
commit;
