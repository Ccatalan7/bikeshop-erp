import 'dart:typed_data';

Future<void> saveAndroidApk({
  required String fileName,
  required List<Uint8List> parts,
}) {
  throw UnsupportedError('APK browser downloads are available on the web.');
}
