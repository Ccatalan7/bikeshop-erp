-- ============================================================================
-- DISABLE EMAIL CONFIRMATION FOR DEVELOPMENT
-- ============================================================================
-- This allows users to sign up and immediately use the app
-- without waiting for email confirmation
-- 
-- ⚠️ IMPORTANT: This is for DEVELOPMENT only!
-- For PRODUCTION, you should enable email confirmation for security
-- ============================================================================

-- Option 1: Via Supabase Dashboard (RECOMMENDED)
-- ============================================================================
-- 1. Go to: Authentication → Settings
-- 2. Scroll to: "Email Confirmation"
-- 3. Toggle OFF: "Enable email confirmations"
-- 4. Click "Save"
--
-- OR
--
-- 1. Go to: Authentication → Settings → Email Templates
-- 2. Under "Confirm signup", check: "Auto-confirm emails"

-- Option 2: Via SQL (if you have database access)
-- ============================================================================
-- NOTE: This requires superuser access and may not work on Supabase hosted
-- You'll likely need to use the Dashboard instead

-- Check current auth settings
-- SELECT name, value FROM auth.config;

-- ============================================================================
-- ALTERNATIVE: Allow specific email domains without confirmation
-- ============================================================================
-- Go to: Authentication → Settings → "Allowed Email Domains"
-- Add: example.com, gmail.com, etc.
-- Set: "Auto-confirm signups from allowed domains"

-- ============================================================================
-- WHAT THIS FIXES:
-- ============================================================================
-- BEFORE (with email confirmation):
-- - User signs up → gets email → clicks link → can login → tenant created on first login
-- - Problem: Tenant not created until email confirmed
-- - Problem: Confusing "error al obtener ID del usuario" message
--
-- AFTER (without email confirmation):
-- - User signs up → immediately logged in → tenant created → redirects to dashboard
-- - Clean, simple flow
-- - No confusing error messages
-- ============================================================================

-- ============================================================================
-- TESTING AFTER CHANGE:
-- ============================================================================
-- 1. Delete test user (if exists): ccatalan.us@gmail.com
-- 2. Sign up again with same email
-- 3. Should immediately:
--    - Create account
--    - Create tenant with subdomain
--    - Initialize default data
--    - Show success message
--    - Redirect to dashboard
-- 4. No email confirmation required!
-- ============================================================================
