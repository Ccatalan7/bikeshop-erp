rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary open rules for development
    // TODO: Add proper authentication and authorization rules for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
