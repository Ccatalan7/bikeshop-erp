# Inventory and Accounting Source Registry

**Status:** P1 source ownership certified; sales corrections, cash settlement, and gradual professional purchase receiving are enforced
**Last verified:** 2026-07-12 against repository code, clean canonical bootstrap, production data, and live trigger/constraint catalog

`Authoritative` means the path owns the stock/accounting posting. `Delegated` means it must create/update an authoritative document and must not post again.

| Source | Entry point | Database path | Stock owner | Accounting owner | Classification / current gap |
|---|---|---|---|---|---|
| Manual sales invoice | `SalesService.saveInvoice()` | `sales_invoices` -> `trg_sales_invoices_change` -> `handle_sales_invoice_change()` | Sales invoice | Sales invoice + payment triggers | Authoritative. Posted line replacement, swaps, quantity edits, retry, reversal, and connected operation evidence are deployed and tested. |
| Sales status/delete | `SalesService.updateInvoiceStatus()` / `deleteInvoice()` | Same sales trigger | Sales invoice | Sales invoice/payment triggers | Authoritative. Posted/non-posted transitions reverse or consume once; unchanged posted payment/status recalculation has zero stock delta. |
| Sales payment / undo | payment forms, POS, Quick Sale, `SalesService.registerPayment()` / `deletePayment()` | `sales_payments` validation + change triggers -> invoice recalculation + payment journal | Intentionally no stock owner | Sales payment | Whole-CLP, row-lock, overpayment, idempotency, connected payment/invoice/job/journal traces, and the zero-stock invariant are live. |
| POS | `POSService.checkout()` | `create_atomic_sales_checkout(source='pos')` creates invoice and split payments in one transaction | Delegated to sales invoice | Delegated to invoice/payment triggers | Atomic/idempotent command and negative-stock guard deployed. Updated app uses it; previous builds retain the compatible legacy route. Historical retry candidates remain review-only. |
| Right-toolbar Quick Sale | `QuickSalePanel` | `create_atomic_sales_checkout(source='quick_sale')` | Delegated to sales invoice | Delegated to invoice/payment triggers | Same atomic kernel as POS with a distinct source and checkout key. Updated app build is required to use it. |
| Workshop job | job form/services, `create_invoice_from_mechanic_job()`, bidirectional sync | Job remains operational; linked `source = mechanic_job` invoice uses normal sales posting | Sales invoice | Sales invoice + payment triggers | Invoice ownership is enforced for Viñabike. Shadow observation covered 9 updated jobs and 7 updated linked invoices with zero job-owned posting attempts; enforcement activation changed no business count or invariant. Multi-bike payments remain one shared invoice/job balance. |
| Online order | Hardened checkout, provider/manual payment, and guided cancellation | `online_orders` -> `process_online_order()` / `confirm_online_order_payment()` / `cancel_online_order()` -> preserved sales invoice | Sales invoice only | Invoice/payment triggers only | Provider and manual-payment replay are hardened. Manual transfer confirmation is row-locked, whole-CLP, reference-backed, and links parent plus invoice/payment operations; a two-session race produced one payment and one stock deduction. Unpaid cancellation preserves evidence; paid corrections route through the invoice. Historical link gaps remain review-only. |
| Purchase invoice | `PurchaseService` and extensions | `purchase_invoices` -> `trg_purchase_invoices_change` | No physical stock in the professional route; temporary untouched-invoice legacy receipt only | Purchase invoice + payment triggers | Authoritative for inventory-value/AP/tax recognition. The live Phase-1 guard prevents restore/reconsume churn and phantom manual movements; the goods-receipt command owns current physical receipt. |
| Purchase goods receipt | Purchase invoice `Recibir productos` | `create_purchase_goods_receipt()` + immutable receipt/line/movement mappings | Goods-receipt command | Explicit accounting no-op: the purchase invoice already owns inventory-value/AP/tax recognition | Viñabike is `enforce`. Current clients post immutable partial/full receipts. During the temporary N/N-1 bridge, an old client may use status receiving only on an invoice with no professional receipt evidence; each use is append-only evidence, and mixed legacy/professional ownership is blocked atomically. |
| Purchase supplier return | Guided workspace from purchase invoice | `create_purchase_supplier_return()` / `void_purchase_supplier_return()` linked to exact receipt movements | Supplier-return command | Physical return journal; supplier settlement remains credit-note-owned | Deployed inactive. Partial/cumulative returns, stock sufficiency, set components, lineage, and void are tested. |
| Purchase credit note | Guided workspace from purchase invoice | `create_purchase_credit_note()` / `void_purchase_credit_note()` | No stock unless linked physical return | Purchase credit-note command | Viñabike `enforce`. Cumulative amount/quantity limits, AP/tax settlement, balanced journal, optional supplier-return claim clearing, and append-only void. Internal document only; not an SII-issued DTE. |
| Supplier cash refund | Purchase credit-note history | `create_purchase_supplier_refund()` / `void_purchase_supplier_refund()` | Intentionally no stock owner | Supplier-refund command | Viñabike `enforce` after authenticated rollback smoke. Records money already verified as received externally, requires whole CLP/reference/reason, preserves gross supplier payments, reduces explicit supplier credit, posts cash/AP and linked reversal journals, and cannot execute a bank transfer. |
| Sales return | Guided workspace from posted sales invoice | `create_sales_return()` / `void_sales_return()` + quarantine resolution commands | Sales-return/disposition command | Physical return command owns inventory/COGS reclassification | Viñabike `enforce`. Supports restock, quarantine, scrap, later release/scrap resolution, cumulative limits, exact source movement, and void guards. |
| Sales credit note | Guided workspace from posted sales invoice | `create_sales_credit_note()` / `void_sales_credit_note()` | No stock unless separately linked to inspected return | Sales credit-note command | Viñabike `enforce`. Cumulative limits and AR/revenue/tax correction without duplicating return inventory/COGS. Internal document only; not an SII-issued DTE. |
| Customer cash refund | Sales credit-note history | `create_sales_customer_refund()` / `void_sales_customer_refund()` | Intentionally no stock owner | Customer-refund command | Viñabike `enforce` after authenticated rollback smoke. Records money already verified as returned externally, requires whole CLP/reference/reason, preserves gross collected payments, reduces explicit customer credit, posts AR/cash and linked reversal journals, and cannot execute a bank transfer. |
| Purchase payment / undo | purchase payment forms, `PurchaseService.createPayment()` / `deletePayment()` | `purchase_payments` validation + change triggers -> purchase status recalculation + payment journal | Intentionally no stock owner | Purchase payment | Live fixes enforce zero payment-driven stock effects, correct partial-prepayment downgrade, paid-before-receipt inventory handling, unique legacy-journal conversion, and connected traces. |
| Manual stock adjustment | `DatabaseService.adjustStock()` / `InventoryService.applyStockAdjustment()` | `apply_manual_stock_adjustment()` or `apply_inventory_stock_adjustment()` | Adjustment RPC | Adjustment RPC | Authoritative. New rows persist exact source identity, actor, balances, movement, journal, and checkpoints. The audit read model distinguishes verified, reconstructed, duplicate, collision, arithmetic-error, and ambiguous legacy evidence. |
| Product form/create/direct stock edit | `InventoryService.updateProduct()` strips stock fields; product creation may carry initial stock; stock UI calls adjustment RPC | `apply_inventory_stock_adjustment()` or `trg_prepare_direct_product_stock_trace` -> legacy adjustment/movement -> `zz_finalize_direct_product_stock_trace` | Adjustment command or compatibility boundary | Same operation; initial stock uses inventory/opening-equity and later direct differences use inventory gain/loss | Supported edits are delegated. Initial-stock and unknown N/N-1/N-2 payloads remain compatible but now receive a complete atomic operation, exact movement, value journal, actor/system identity, and invariant checkpoint. One-column writes are synchronized; disagreeing, negative, and cross-tenant writes fail atomically. |
| Mass product edit | `BulkProductEditService` | child adjustment RPCs + `link_product_bulk_edit_operations()` | Adjustment RPC per child | Adjustment RPC per child | Deployed parent operation links every successful row and records explicit partial-success policy. |
| Imports | `ProductImportService` | zero-stock product create/update + `apply_product_import_stock()` | Import stock command | Adjustment kernel | Deployed command uses absolute target, durable idempotency, exact child operation, and rejects stocked product-to-service conversion. |
| Product conversion | Conversion/restore RPCs | conversion RPCs + `trg_trace_product_conversion_activity` | Conversion RPC | Conversion RPC | Deployed trace joins movement, adjustment, journal, original conversion, and restoration under common operation lineage. |
| Sets/components | Sales, receipt, supplier return, returns | Owning commands explode `product_set_components` | Owning document | Owning document | Direct/set component quantities and exact movement mappings are covered by database tests. |
| Legacy Zoho/admin product scripts | `scripts/zoho_import/{import_all_zoho_products,import_one_new_product,import_two_products_with_images,test_single_product_import,test_import_with_tracking,test_force_change}.py`, `scripts/import_templates/sync_zoho_to_flutter.py`, `scripts/import_templates/connections/supabase_connection.py`, `scripts/seed_bikes_wheels.py`, and `scripts/zoho_import/debug_context.sql` | Direct `products` insert/update -> `20260712150000` compatibility boundary | Compatibility boundary | Compatibility boundary | Legacy-to-remove, but no longer a silent bypass when executed through application/service-role traffic. Modern in-app import remains the preferred idempotent absolute-stock command. Direct DBA sessions are maintenance-only and must follow the backed-up audit runbook. |
| Legacy `orders/order_items` | No supported UI writer | Former direct stock trigger is disabled by `20260711123000` | None | None | Legacy bypass closed; supported channels delegate to sales invoices. |
| Transfers/reservations | No operational multi-location stock model | No writer | None | None | Explicitly unsupported, therefore not a current stock source. Must be a first-class command before UI exposure. |
| Invoice delete / journal replacement | Invoice lists/forms and database status handlers | Draft-only delete guard + `journal_supersession_evidence` before legacy journal replacement | Invoice trigger | Invoice/payment trigger with immutable full journal snapshot | Posted invoice deletion is blocked. Every legacy posted-journal replacement preserves the full header, all lines, source operation, actor, and reversal checkpoint before deletion. |
| Factory reset/admin/backfill | Settings service, migrations, scripts | Client financial/inventory purge is disabled; repair scripts are preview-only | Administrative | Administrative | Partial client-side purge is refused. A future purge must be one backed-up, atomic, audited maintenance command. Historical repair remains approval-gated and has not run. |

## Production facts captured on 2026-07-11

- Production had 1,655 products, 2,474 stock movements, 1,205 stock adjustments, 2,137 journals, 775 sales invoices, and 75 purchase invoices immediately before and after the inactive release.
- Product stock columns, posting-order ledger continuity, current-vs-latest-ledger balance, and journal balance all had zero drift after deployment.
- Twenty tracked products already had negative stock. The release prevents additional sales overselling but did not infer or rewrite those historical balances.
- The two product stock columns had zero current drift at inspection time.
- Sixty manual adjustments had no actor.
- No duplicate invoice journal headers or unbalanced journals were found by the initial aggregate checks.
- Product SKU `4715575883212` had current stock `6`, movement net `-10`, and 65 movement rows.
- Purchase invoice `208049` produced real reversal rows and phantom manual decreases at the same timestamps, proving the missing automatic-update guard.
- Production `handle_sales_invoice_change()` explicitly skipped stock for posted-to-posted edits.
- Repository trigger inspection exposes legacy job stock/journal writers, but their current handler returns before its deferred posting block. Live data confirms zero job-owned stock movements and zero job-owned revenue journals across 398 jobs; 396 are invoice-linked. The linked invoice is therefore the existing de facto posting owner.
- The July payment hardening is active: current production has zero fractional/non-positive/overpaid/exact-duplicate purchase payments and zero received-date/status drift.
- The historical payment fix was incomplete at the inventory boundary: 92 distinct phantom manual adjustments across 20 purchase invoices match purchase reversal timestamps. Invoice `208049` has four such rows.
- Of 71 active purchase payments, 69 still use legacy invoice-number journal linkage, one uses payment-ID linkage, and one (`FC-00015`, payment `c9eb167e-b19f-4f58-ac50-7ca596df8d8d`) has no recognized payment journal. Invoices `138518` and `AE010625` each have two duplicate legacy payment journals. The live handler safely converts only a unique amount-matched legacy journal and blocks ambiguous edits for audit review. No historical repair has been run.
- Production has 12 jobs with 2+ bicycles; all are currently fully paid and their job paid flags agree with invoice status. Their 67 invoice item rows include 25 historical rows without `job_bike_id`, so per-bike attribution is incomplete even though no invalid foreign bicycle IDs were found.
- Current linked-invoice reconciliation found 117 legacy product-level movement variances across 82 jobs. They are classified as `legacy_unresolved`; many involve historical service/product reclassification, while `FV-00809` includes the known posted-edit gap. No correction is inferred from this comparison.
- The movement audit read model has one row per raw movement (2,474/2,474 at deployment). For SKU `4715575883212`, it now shows the proven adjustment balances `9 → 0` and `10 → 0`, identifies their purchase-reversal collision, preserves raw quantities, and excludes only the exact duplicate technical footprint from summaries.
- Production drift had left `stock_movements.created_by` and `journal_entries.created_by` referencing the empty legacy `users_profiles` table. This blocked Quick Sale after actor tracing was activated. Migration `20260711190000` points both to `auth.users`; a rollback-only production smoke with the affected employee ID passed and business totals were unchanged.
- The inactive cash-settlement installation on 2026-07-12 preserved 1,654 products, 2,471 movements, 1,203 adjustments, 2,136 journals, 771 sales invoices, 74 purchase invoices, and all payment counts exactly. Both refund controls remained disabled; no refund, movement, journal, or invoice settlement amount was created. Stock-column drift, posting-ledger continuity, current-ledger drift, and unbalanced journals remained zero.
- Workshop invoice ownership moved from `shadow` to `enforce` on 2026-07-12 after 9 jobs and 7 workshop invoices changed under observation with zero job-owned movement/journal attempts. Activation preserved 1,654 products, 2,471 movements, 1,203 adjustments, 2,136 journals, 771 sales invoices, and 400 jobs; drift, imbalance, and control events remained zero.
- Customer and supplier refund commands passed an authenticated production rollback smoke covering create, journal balance, invariant checkpoints, zero stock, preserved gross payments, and void/reversal. Both controls were then activated separately; refund rows stayed zero, movements stayed 2,471, journals stayed 2,136, and stock/journal invariants remained clean.
- The direct-product writer boundary passed an authenticated production rollback smoke for a real employee/product: one-column input synchronized both balances, created exactly one operation/adjustment/movement/balanced journal, passed its invariant checkpoint, and cleared its multi-row context bridge. The transaction was rolled back. Installation preserved 1,654 products, 2,471 movements, 1,203 adjustments, 2,136 journals, 771 sales invoices, 74 purchase invoices, 732 sales payments, 71 purchase payments, and 45 completed operations. Stock drift, arithmetic errors, ledger-chain breaks, current/latest drift, journal imbalance, incomplete operations, and post-July-11 untraced movements all remained zero.
- Gradual purchase receiving passed an authenticated rollback-only production smoke covering professional partial receipt, exact operation/movement, old-client receipt on an untouched invoice, actor-backed compatibility evidence, and atomic mixed-route rejection. Viñabike then moved to `enforce` with the temporary untouched-invoice bridge. Activation preserved 1,654 products, 2,471 movements, 1,203 adjustments, 2,136 journals, and 74 purchase invoices; professional receipts and compatibility events remained zero, and every stock/accounting invariant remained zero.

## Remaining rollout work

- Observe new online cancellation/manual-payment operations; historical link gaps remain review-only.
- Observe gradual purchase receiving for 7-14 operating days. Update every receipt-capable workstation, investigate any compatibility event, and disable `legacy_untouched_compatibility` only after the reviewed window is clean.
- Design transfers/reservations only when the business introduces multiple on-hand locations or reservation allocation.
